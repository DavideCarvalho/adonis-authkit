---
title: Account Lockout
description: Progressive per-email lockout, complementary to IP rate-limiting.
---

Account lockout is an anti-brute-force throttle keyed by **email** — complementary to the
IP-keyed [rate-limiting](/docs/security). After repeated password failures, the account is
temporarily locked with a progressive (backing-off) duration.

It is backed by the same `@adonisjs/limiter` peer as rate-limiting — **no migration, no
extra table** — and is **on by default**. If `@adonisjs/limiter` is not configured, it
degrades to a no-op (fail-safe).

## Configuration

```ts title="config/authkit_server.ts"
defineConfig({
  // ...
  lockout: {
    enabled: true,
    maxAttempts: 5,
    windowSec: 900,       // 15 min sliding window to count failures
    baseLockoutSec: 60,   // first lockout duration
    maxLockoutSec: 3600,  // ceiling of the progressive backoff
    // store: 'redis',    // a store from config/limiter.ts
  },
})
```

| Field | Type | Default | Notes |
| --- | --- | --- | --- |
| `enabled` | `boolean` | `true` | No-op when the limiter isn't configured. |
| `maxAttempts` | `number` | `5` | Failures within the window before locking. |
| `windowSec` | `number` | `900` | Sliding window (seconds) for counting failures. |
| `baseLockoutSec` | `number` | `60` | Duration of the first lockout. |
| `maxLockoutSec` | `number` | `3600` | Ceiling for the progressive backoff. |
| `store` | `string?` | host default | A store name from `config/limiter.ts`. |

## Behaviour

On the password step of the login interaction:

- Each failed password attempt is recorded for the email; after `maxAttempts` within
  `windowSec`, the email is locked for an increasing duration (starting at `baseLockoutSec`,
  backing off up to `maxLockoutSec`).
- While locked, the login is rejected with the `errors.account_locked` message
  (interpolated with the remaining `{seconds}`) and an `account.locked`
  [audit event](/docs/security) is emitted.
- A **successful** password clears the failure counter for that email.

<Callout type="info">
  Lockout (per-email) and rate-limiting (per-IP) defend different vectors and stack: the IP
  bucket slows a single host hammering the login route, while lockout slows a distributed
  attack spread across IPs but targeting one account.
</Callout>
