---
icon: LayoutDashboard
title: Admin Console
description: The opt-in IdP admin console — a React SPA (default) or Edge server-rendered, with dashboard, users, sessions, orgs, clients, audit, and settings.
---

AuthKit ships an opt-in **admin console**: a metrics dashboard, user/role management,
session context, organization management, the OAuth client list, the audit log, an
impersonation panel, and a full runtime settings editor. It is **off by default** —
existing hosts don't change behaviour.

## Enabling it

The console must be turned on in **both** places — `defineConfig` (the guard) and
`registerAuthHost` (route mounting happens before the lazy config resolves):

```ts title="config/authkit_server.ts"
defineConfig({
  // ...
  admin: {
    enabled: true,
    roles: ['ADMIN'], // global roles allowed into /admin; default ['ADMIN']
  },
})
```

```ts title="start/routes.ts"
registerAuthHost(router, {
  mountPath: '/oidc',
  admin: true, // console under /admin
})
```

| Field | Type | Default | Notes |
| --- | --- | --- | --- |
| `enabled` | `boolean` | `false` | Mounts the admin console group behind the admin guard. |
| `roles` | `string[]` | `['ADMIN']` | Global roles that grant access to the console. |

### Configurable admin prefix

By default the console is mounted under `/admin`. You can move it to any path by passing
`{ prefix: '...' }` instead of `true` in `registerAuthHost`:

```ts title="start/routes.ts"
// Default — console under /admin (backward-compatible)
registerAuthHost(router, {
  mountPath: '/oidc',
  admin: true,
})

// Custom prefix — console under /auth/admin
registerAuthHost(router, {
  mountPath: '/oidc',
  admin: { prefix: '/auth/admin' },
})
```

The prefix is normalised automatically: it always starts with `/` and never has a trailing
slash. All console routes, API calls, and assets follow the effective prefix — no further
configuration is required. The Admin REST API (`/api/authkit/v1`) is **not** affected by
this option.

**Acme example** — Acme mounts the console at `/auth/admin` to avoid collisions with an
existing `/admin` route in their main app:

```ts title="start/routes.ts"
registerAuthHost(router, {
  mountPath: '/oidc',
  admin: { prefix: '/auth/admin' },
  adminApi: true,
})
```

The dashboard is then at `https://auth.acme.com/auth/admin`. The SPA keeps all navigation
and filter state in the URL query string via [nuqs](https://nuqs.47ng.com/) (`?view=users`,
`?view=users&q=ana&page=2`, …), so every page and filter is deep-linkable and the same shell
at `{prefix}` serves them all.

<Callout type="info">
  When `prefix` is omitted inside the object form (`admin: {}`), the default `/admin` is
  used — identical to `admin: true`.
</Callout>

### The SPA under the hood

The console is a self-contained, Vite-built React application (nuqs URL state, dark/light
theme, violet accent `#625fff`) bundled **inside** the npm package — zero extra build step
for the host. It is:

- **Pre-built and bundled inside `@adonis-agora/authkit-server`** — the Vite output lands
  in `build/src/host/ui-dist/` at publish time and is served directly by `AdminShellController`.
  No separate build, no Vite dev server, no extra npm scripts on the host.
- **nuqs URL state** — page navigation (`?view=users`) and per-page filters (search,
  pagination, the open detail drawer) live in the query string via
  [nuqs](https://nuqs.47ng.com/)'s generic React adapter (`nuqs/adapters/react`). Every view
  and filter combination is deep-linkable and survives refresh; switching pages clears the
  shared filter params so state never leaks between views. Ephemeral UI (modals, forms)
  stays in React state.
- **JSON API + TanStack Query** — the SPA fetches all data client-side via
  `@adonis-agora/authkit-react`'s typed client and TanStack Query hooks
  (`useUsersQueryOptions`, `useCreateUserMutationOptions`, etc.). The API endpoints live
  under `{prefix}/api/*` and are protected by the same `adminGuard` as the shell.
  See **[Typed Client & TanStack Query](/docs/data-fetching)** for the full hook catalog —
  the SPA is the canonical dogfood implementation; if you are building a custom admin
  interface, use the same hooks.
- **`AuthkitClientProvider`** — the shell wraps the app in `<QueryClientProvider>` and
  `<AuthkitClientProvider>`. The client reads `window.__AUTHKIT__.endpoints.api` for the
  base URL and `window.__AUTHKIT__.csrfToken` for CSRF — no hard-coded paths anywhere.
- **Assets** — `{prefix}/assets/:file` serves the hashed JS/CSS files with
  `Cache-Control: public, max-age=31536000, immutable`.
- **Dark/light theme** — respects the OS preference (`prefers-color-scheme`). Accent colour
  `#625fff`.
- **`window.__AUTHKIT__` config** — the shell injects a config object at render time
  (`adminBase`, `csrfToken`, `currentUser`, `endpoints.api`). The SPA never hard-codes paths.

If the built dist is absent (a dev environment before running the server build), the shell
falls back to a minimal HTML page with build instructions.

## The guard

The `adminGuard` (also exported) protects the group. It requires:

1. `admin.enabled: true` — when the console is **disabled**, the guard responds with **404
   Not Found** (the routes simply don't exist as far as a caller can tell);
2. an active **account session** (otherwise redirect to `/account/login`);
3. the signed-in account having at least one of `admin.roles` in its global roles
   (otherwise redirect to the configured `accountHome` — default `/account/security` — so the existence of `/admin` is not leaked).

The allowed roles are resolved at runtime from the `authkit.server` config. See
[Security](/docs/security) for how the disabled-console 404 fits the broader hardening.

## Routes


The SPA shell is served for every page route; actual data is fetched from JSON API endpoints
under `{prefix}/api/*`. All endpoints are protected by the `adminGuard`.

| Route | Notes |
| --- | --- |
| `GET {prefix}` | Shell HTML — SPA loads, [nuqs](https://nuqs.47ng.com/) drives URL state |
| `GET {prefix}/*` | Shell HTML — catches all SPA deep links |
| `GET {prefix}/assets/*` | Vite-built JS/CSS/fonts (hashed names, `Cache-Control: immutable`) |
| `GET {prefix}/api/overview` | Dashboard metrics (total users, MAU, sessions, daily charts) |
| `GET {prefix}/api/users` | List/search users |
| `GET {prefix}/api/users/:id` | User detail |
| `POST {prefix}/api/users` | Create user |
| `PATCH {prefix}/api/users/:id/roles` | Update roles |
| `POST {prefix}/api/users/:id/disable` | Disable account |
| `POST {prefix}/api/users/:id/enable` | Enable account |
| `POST {prefix}/api/users/:id/reset-password` | Send password reset email |
| `DELETE {prefix}/api/users/:id` | Delete user (cascade + anonymize) |
| `GET {prefix}/api/users/:id/sessions` | List a single user's active sessions |
| `POST {prefix}/api/users/:id/revoke-sessions` | Disconnect a user from all devices (revoke all their sessions + grants) |
| `GET {prefix}/api/sessions` | List active sessions/grants (all accounts) |
| `POST {prefix}/api/sessions/revoke-all` | Revoke all sessions for an account |
| `GET {prefix}/api/clients` | List adapter-stored OIDC clients |
| `POST {prefix}/api/clients` | Create a dynamic client |
| `PATCH {prefix}/api/clients/:id` | Edit a dynamic client |
| `DELETE {prefix}/api/clients/:id` | Delete a dynamic client |
| `POST {prefix}/api/clients/:id/regenerate-secret` | Regenerate client secret |
| `GET {prefix}/api/roles` | List the roles catalog |
| `POST {prefix}/api/roles` | Add a role to the catalog |
| `PATCH {prefix}/api/roles/:name` | Update a catalog role |
| `DELETE {prefix}/api/roles/:name` | Remove a catalog role |
| `GET {prefix}/api/orgs` | List organizations |
| `GET {prefix}/api/orgs/:id` | Org detail |
| `GET {prefix}/api/audit` | Browse audit log |
| `GET {prefix}/api/settings` | Read all runtime settings |
| `PUT {prefix}/api/settings/:key` | Upsert a setting |
| `DELETE {prefix}/api/settings/:key` | Delete a setting (reset to lib default) |
| `GET {prefix}/api/impersonation/:userId` | Get impersonation panel data |

## Dashboard

`GET /admin` shows a metrics overview built from the audit sink:

- **Total users** — count from the account store.
- **Active sessions** — count of current sessions (when the adapter supports enumeration).
- **MAU (Monthly Active Users)** — unique accounts with a `login.success` in the last 30 days.
- **Daily sign-ins / sign-ups** — server-side SVG line charts for the last 30 days.

When the audit sink does not support `list`, the charts are omitted and only the static
counts are shown.

## Session context

Every session displayed in the console (user detail page) carries rich context captured
at login time:

| Field | Source |
| --- | --- |
| Browser | Parsed from the User-Agent string (e.g. `Chrome 124`) |
| OS | Parsed from the User-Agent string (e.g. `macOS`) |
| IP address | Request IP at login |
| Location | Resolved via the host's `resolveGeo` hook (optional) |

To add geolocation, plug in a `resolveGeo` hook in `defineConfig`:

```ts title="config/authkit.ts"
resolveGeo: async (ip) => {
  // example: return 'São Paulo, BR'
  const geo = await myGeoLookup(ip)
  return geo ? `${geo.city}, ${geo.countryCode}` : null
},
```

The lookup is **fail-safe with a 1.5 s timeout**: an error or timeout returns `null` (no
location shown) and never delays the listing.

## Sessions & revocation

The user detail drawer lists the account's active **sessions** (IdP logins) and **grants**
(per-client authorizations), with a live count of access / refresh tokens per grant
(`GET {prefix}/api/users/:id/sessions`). The drawer's **Actions** row exposes a
**"Disconnect all devices"** button that revokes every session and grant for that single
user (`POST {prefix}/api/users/:id/revoke-sessions`) — the admin-side equivalent of the
self-service "Sign out of all devices" action on `/account/security`.

The global **Sessions** page additionally offers a **"Revoke all sessions and grants"**
button across all accounts, served by `AdminSessionsService`.

Revocation works by destroying the account's `Session` and `Grant` artifacts directly
through the OIDC adapter. Destroying a grant **cascades** to its tokens: oidc-provider
validates every access/refresh token against `Grant.find(token.grantId)` and rejects it
with `invalid_token` when the grant is gone. As a belt-and-braces measure the service also
destroys the access/refresh token rows that reference the revoked grants (when the adapter
can enumerate them). The operation emits a `session.revoked_all` audit event with the
counts in `metadata`.

Like the client list, enumeration relies on the adapter's **optional** `list?()` capability
(the database and Redis adapters both implement it). When the adapter can't enumerate, the
page **degrades gracefully** with a notice instead of erroring. Both the database adapter's
table query and the Redis adapter's `SCAN` are scoped per model, so the lookup is cheap.

## Impersonation panel

When the `admin_impersonation` runtime setting is enabled (`{ "enabled": true }`), a user's
detail page shows an **Impersonation** panel with the exact `token_exchange` parameters
needed to assume that user's identity (RFC 8693). This surfaces the `subject_token_type`,
`requested_token_type`, and a ready-to-run `curl` snippet — it does **not** perform the
exchange itself.

```bash
# Enable via runtime setting (no redeploy)
node ace authkit:settings:set admin_impersonation '{"enabled":true}'
```

<Callout type="warn">
  The exchange requires a confidential client with the `urn:ietf:params:oauth:grant-type:token-exchange`
  grant and a valid admin access token as `subject_token`. The panel shows the parameters;
  your client performs the actual request. Viewing the panel emits an
  `impersonation.started` audit event.
</Callout>

## User management (create / invite / reset / disable / delete)

The users page has a **create** form (email + optional name + optional password). When you
leave the password blank the account is created with a throwaway password and a
**password-reset email** is sent — the user sets their own password (the "invite" flow).
Each row also exposes **Send password reset** and, when the store supports it,
**Disable** / **Enable** buttons. The operations emit `user.created`, `user.password_reset_sent`, `user.disabled`,
`user.enabled`, and `user.deleted` audit events. The **Delete** action runs the same cascade
as the self-service flow: sessions, grants, PATs, passkeys, provider identities, and org
memberships are removed; audit records are anonymized. See [Compliance](/docs/compliance)
for the full cascade details.

Disabling requires the optional **`AccountStatusCapability`** on the account store (see
[Account store](/docs/account-store)). The Lucid store implements it via a
`disabled_at` timestamp column **only when the column exists on your model** — otherwise the
capability is genuinely absent and the disable/enable buttons are hidden. Add the column with
a migration of your own:

```ts
table.timestamp('disabled_at').nullable()
```

Both login paths (the OIDC interaction and the account-console login) reject disabled
accounts with an "account disabled" message, even with a correct password.

## Client CRUD

All clients are managed at runtime — there is no static client list in config. Create them
via this console, the [Admin REST API](/docs/admin-api#clients), or the
`authkit:clients:create` command (see [Reference](/docs/reference#authkitclientscreate)).
No redeploy needed.

The clients page lists all **adapter-stored** clients created via the
console or [Dynamic Registration](/docs/dynamic-registration). Adapter-stored clients are
fully manageable via `AdminClientsService`:

- **Create** — generates a `client_id` when omitted; for confidential clients
  (`tokenEndpointAuthMethod !== 'none'`) it generates a `client_secret` and shows it **once**
  (it isn't recoverable afterwards). Public clients get no secret.
- **Edit** — updates the editable metadata (redirect URIs, post-logout redirect URIs,
  grants, token-endpoint auth method, backchannel logout URI, `session_required`) while
  **preserving** the existing secret.
- **Regenerate secret** — issues a fresh `client_secret` for a confidential client (shown
  once); rejected for public clients.
- **Delete** — removes the client from the adapter.

Every write builds the payload in the exact snake_case shape the underlying oidc-provider
persists (matching what RFC 7591 dynamic registration writes) and then **evicts the
provider's dynamic-client cache** so the change takes effect immediately. Each operation
emits an audit event: `client.created`, `client.updated`, `client.deleted`.

Enumeration relies on the adapter's **optional** `list?()` capability (the legacy
`listClients?()` is still honored for back-compat and now delegates to `list()`). When the
adapter implements neither, the service's `canList` is `false` and the page **degrades
gracefully** — the create form still works, but the adapter-stored list is hidden with a
notice rather than erroring.

## Organizations

When the three organization tables are present, an **Organizations** tab appears at
`/admin/orgs`. See [Organizations](/docs/organizations#admin-console----adminorgs) for
the full route table and behaviour.

### Organization management

The Organizations tab supports the full CRUD lifecycle:

- **Create** — form with display name, slug, and optional logo URL.
- **Edit** — rename and update slug/logo. The slug must be unique and URL-safe.
- **Members** — list current members, change roles, and remove members.
- **Invitations** — create email invitations and revoke pending ones.
- **Danger zone** — delete the organization (cascades membership rows; does not delete member accounts).

### Org-scoped settings (Settings tab in the org drawer)

Opening an org and selecting the **Settings** tab shows two cards that can be overridden
per-organization:

| Card | Setting key | What it controls |
| --- | --- | --- |
| Organization Policy | `organizations_policy` | `allowSelfCreate`, `invitationTtlHours`, `roles` list |
| Roles Catalog | `roles_catalog` | Custom role definitions available within this org |

Saving an org-scoped value creates a row in `auth_settings` with `organization_id` set to
the org's ID. The resolution chain is `org → global → lib default`. See
[Runtime Settings — Org-scoped settings](/docs/settings#org-scoped-settings) for details.

## Settings page

`{prefix}#/settings` shows a **Runtime
Settings** panel with multiple sections covering all 21 known setting keys. The page is the
primary interface for operational configuration that does not require a redeploy. See
[Runtime Settings](/docs/settings) for the full setting catalog, precedence rules, and
migration guide.

Every card shows a **badge** indicating whether the effective value comes from the runtime
setting (`from setting`) or falls back to the lib default (`from default`). When the
`auth_settings` table is absent, a notice appears with the `CREATE TABLE` snippet and submit
buttons are disabled.

Every save emits a `settings.updated` audit event.

### Section: Security & Access

Contains cards for `bot_protection`, `registration`, `require_verified_email`,
`maintenance_mode`, and `auth_methods`.

| Route | Action |
| --- | --- |
| `GET {prefix}/settings` | Render the settings page. |
| `POST {prefix}/settings/bot-protection` | Save (upsert) the `bot_protection` setting. |
| `POST {prefix}/settings/bot-protection/reset` | Delete the setting — reverts to static config. |
| `POST {prefix}/settings/registration` | Save (upsert) the `registration` setting. |
| `POST {prefix}/settings/registration/reset` | Delete the setting — reverts to static config. |
| `POST {prefix}/settings/require-verified-email` | Save (upsert) the `require_verified_email` setting. |
| `POST {prefix}/settings/require-verified-email/reset` | Delete the setting — reverts to static config. |
| `POST {prefix}/settings/maintenance-mode` | Save (upsert) the `maintenance_mode` setting. |
| `POST {prefix}/settings/maintenance-mode/reset` | Delete the setting — disables maintenance mode. |
| `POST {prefix}/settings/auth-methods` | Save (upsert) the `auth_methods` setting. |
| `POST {prefix}/settings/auth-methods/reset` | Delete the setting — reverts to config-derived defaults. |

#### Bot-protection card

The card shows the current effective state and lets an admin:

- **Enable / disable** bot protection for the whole IdP without a redeploy.
- **Override the protected flows** (`login`, `signup`, `reset`) independently of the
  config value.

**States:**

- **Config absent (`botProtection.verify` not set)** — the card is rendered in a disabled
  state with a notice that bot protection has not been wired up in `config/authkit.ts`. The
  form controls are non-interactive.
- **`auth_settings` table absent** — a notice explains that the `auth_settings` table is
  required for runtime toggles and shows the `CREATE TABLE` snippet. The form still
  renders so the admin can see the static-config values, but submit buttons are disabled.
- **Table present** — the form is live. Submitting it writes (or deletes) the
  `bot_protection` row and every subsequent request picks up the change within 15 s (the
  TTL of the in-process cache). A **Reset to config** button deletes the row and returns
  control to the static config.

See [Security — Runtime toggle](/docs/security#runtime-toggle) for the full precedence
rules and setting shape.

#### Registration card

Controls whether public sign-up is open. The static fallback is `config.registration.enabled`
(default `true`). Disabling closes the sign-up page and rejects new registrations; org
invitations and admin-created accounts are unaffected. A **Reset to config** button
removes the runtime setting and returns to the static default.

See [Security — Registration toggle](/docs/security#registration-toggle) for the full
behaviour and escape hatch.

#### Require verified email card

Overrides `config.login.requireVerifiedEmail` at runtime for all three login flows. Includes
an optional **grace period** field (`graceDays`) that allows newly-registered accounts to
log in for a configurable number of days without a verified email. When the `accountStore`
lacks the `isEmailVerified` capability the card shows a notice that the guard is a no-op.

See [Compliance — Email verification gate](/docs/compliance#verified-email-grace) for the
grace-period behaviour and [Security — Require verified email toggle](/docs/security#require-verified-email-toggle)
for the precedence rules.

#### Maintenance mode card

Activates a maintenance screen that blocks login / sign-up / forgot-password for
non-admin users. An optional custom message is shown on the maintenance page; leave it
blank to use the built-in i18n string. Admin accounts (roles matching `admin.roles`)
bypass the gate.

A **Reset** button removes the setting and takes the IdP out of maintenance mode. If the
console is unreachable during an incident, the Admin REST API escape hatch lets you disable
maintenance without a session:

```bash
curl -X PUT https://idp.acme.com/api/authkit/v1/settings/maintenance_mode \
  -H "Authorization: Bearer $KEY" \
  -H "Content-Type: application/json" \
  -d '{ "value": { "enabled": false } }'
```

See [Security — Maintenance mode](/docs/security#maintenance-mode) for the full behaviour.

#### Authentication methods card

Controls which login methods the sign-in screen offers at runtime — without a redeploy.

The card lists checkboxes for each method with contextual dependency hints:

- **Password** — the standard email + password form.
- **Magic link** — "send me a login link"; shown only when the mail capability and store
  support it. The checkbox is disabled with a hint when the capability is absent.
- **Passkey** — passkey-first sign-in; shown only when WebAuthn is configured.
- **Forgot password** — the "forgot my password" link. The checkbox is automatically
  disabled (and unchecked) when **Password** is off — there is nothing to reset without a
  password.
- **Social providers** — one checkbox per configured provider (e.g. Google, GitHub). Only
  providers present in the static config are listed; the setting cannot activate a provider
  the code does not have wired up.

A **Reset to config** button deletes the row and returns to the config-derived defaults.

See [Security — Authentication methods](/docs/security#authentication-methods) for the full
shape, precedence rules, and fail-safe behaviour.

### Section: Email Change

Card for the `email_change` setting. Controls whether users can change their email address
and the parameters of the verified double-confirmation flow.

| Route | Action |
| --- | --- |
| `POST {prefix}/settings/email-change` | Save (upsert) the `email_change` setting. |
| `POST {prefix}/settings/email-change/reset` | Delete the setting — reverts to defaults. |

See [Compliance — Verified email-change flow](/docs/compliance#email-change) for the full
flow, mail hooks, and capability requirements.

### Section: Security Notifications

Card for the `security_notifications` setting. Controls automatic security-alert emails
triggered by sensitive account events.

| Route | Action |
| --- | --- |
| `POST {prefix}/settings/security-notifications` | Save (upsert) the `security_notifications` setting. |
| `POST {prefix}/settings/security-notifications/reset` | Delete the setting — reverts to defaults (all enabled). |

The card shows checkboxes for each of the six notification kinds (`password_changed`,
`mfa_enabled`, `mfa_disabled`, `passkey_added`, `passkey_removed`, `email_changed`). All
are enabled by default.

See [Compliance — Security notifications](/docs/compliance#security-notifications) for
the full kind list, mail hook, and audit details.

### Section: Passwords

Cards for `password_history`, `password_expiration`, and `password_policy` (which now
includes `blockCommon` — see [Passwords & Migration](/docs/passwords-and-migration#common-passwords)).

| Route | Action |
| --- | --- |
| `POST {prefix}/settings/password-history` | Save (upsert) the `password_history` setting. |
| `POST {prefix}/settings/password-history/reset` | Delete — disables history enforcement. |
| `POST {prefix}/settings/password-expiration` | Save (upsert) the `password_expiration` setting. |
| `POST {prefix}/settings/password-expiration/reset` | Delete — disables expiration enforcement. |
| `POST {prefix}/settings/password-policy` | Save (upsert) the `password_policy` setting. |
| `POST {prefix}/settings/password-policy/reset` | Delete — reverts to config/lib defaults. |

The password-history card shows a notice when the `auth_password_history` table is absent
(capability not detected). The password-expiration card shows a notice when the
`password_changed_at` column is absent.

See [Passwords & Migration](/docs/passwords-and-migration) for the full capability setup,
migration SQL, and behaviour details.

### Section: Sessions & Tokens

Cards for `session_policy`, `trusted_devices`, `notifications`, and `token_ttl`.

| Route | Action |
| --- | --- |
| `POST {prefix}/settings/session-policy` | Save (upsert) the `session_policy` setting. |
| `POST {prefix}/settings/session-policy/reset` | Delete — reverts to defaults. |
| `POST {prefix}/settings/trusted-devices` | Save (upsert) the `trusted_devices` setting. |
| `POST {prefix}/settings/trusted-devices/reset` | Delete — reverts to lib defaults. |
| `POST {prefix}/settings/notifications` | Save (upsert) the `notifications` setting. |
| `POST {prefix}/settings/notifications/reset` | Delete — reverts to defaults. |
| `POST {prefix}/settings/token-ttl` | Save (upsert) the `token_ttl` setting. |
| `POST {prefix}/settings/token-ttl/reset` | Delete — reverts to lib defaults. |

**Session policy card** shows fields for:
- **Remember me** — enable/disable the "keep me signed in" checkbox and its duration.
- **Default session hours** — session duration when remember-me is unchecked (or disabled).
- **Single session** — when enabled, logging in revokes all other active sessions for the
  account. Emits `session.single_enforced` audit events.
- **Idle timeout** — inactivity timeout for the account console (minutes). 0 = disabled.
  See the [known limitation](#idle-timeout-limitation) below.

<Callout type="info">
  **Idle timeout limitation:** the idle timeout is tracked via the Adonis session on the
  account console routes. It does **not** affect the OIDC side — refresh tokens have
  their own lifetime controlled by `token_ttl.refreshTokenSec`. This limitation is
  intentional and documented: the oidc-provider session cannot be interrupted per-request
  without a full logout.
</Callout>

**Token TTL card** shows numeric inputs for `accessTokenSec`, `idTokenSec`, and
`refreshTokenSec`. Changes take effect immediately via mutable holders (the oidc-provider
TTL functions read the holder synchronously). Session TTL is in the Session Policy card.

### Section: Admin & Organizations

Cards for `admin_impersonation`, `organizations_policy`, `lockout`, and `rate_limit`.

| Route | Action |
| --- | --- |
| `POST {prefix}/settings/admin-impersonation` | Save (upsert) the `admin_impersonation` setting. |
| `POST {prefix}/settings/admin-impersonation/reset` | Delete — reverts to lib defaults. |
| `POST {prefix}/settings/organizations-policy` | Save (upsert) the `organizations_policy` setting. |
| `POST {prefix}/settings/organizations-policy/reset` | Delete — reverts to lib defaults. |
| `POST {prefix}/settings/lockout` | Save (upsert) the `lockout` setting. |
| `POST {prefix}/settings/lockout/reset` | Delete — reverts to lib defaults. |
| `POST {prefix}/settings/rate-limit` | Save (upsert) the `rate_limit` setting. |
| `POST {prefix}/settings/rate-limit/reset` | Delete — reverts to lib defaults. |

The **rate_limit card** shows a notice about the known limitation: the route throttle
middleware uses boot-time values; only the lockout-side (AccountLockout) is affected by
the runtime setting.

The **organizations policy card** always includes `owner` in the roles list — the `owner`
role is an invariant and cannot be removed even if the setting omits it.

### Section: Account Security

Cards for `sudo_mode`, `otp_lockout`, and `account_expiration` (new in this release).

| Route | Action |
| --- | --- |
| `POST {prefix}/settings/sudo-mode` | Save (upsert) the `sudo_mode` setting. |
| `POST {prefix}/settings/sudo-mode/reset` | Delete — reverts to lib defaults (enabled, 15 min grace). |
| `POST {prefix}/settings/otp-lockout` | Save (upsert) the `otp_lockout` setting. |
| `POST {prefix}/settings/otp-lockout/reset` | Delete — reverts to lib defaults (enabled, 5 attempts, 24 h). |
| `POST {prefix}/settings/account-expiration` | Save (upsert) the `account_expiration` setting. |
| `POST {prefix}/settings/account-expiration/reset` | Delete — reverts to lib defaults (disabled). |

See [Security — Sudo mode](#sudo-mode-and-otp-lockout) and
[Compliance — Account expiration](/docs/compliance#account-expiration) for full details.
